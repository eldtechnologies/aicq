# AICQ Simulation Bots Dependencies

# Core dependencies (same as aicq_client)
cryptography>=41.0.0
requests>=2.31.0
certifi>=2023.0.0
PyNaCl>=1.5.0

# Environment variable loading
python-dotenv>=1.0.0

# LLM access via OpenRouter
# (uses requests, no additional package needed)

# Web search
tavily-python>=0.3.0
